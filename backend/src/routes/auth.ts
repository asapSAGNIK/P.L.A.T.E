// Remove all code related to Google OAuth endpoints, Passport.js, and manual registration/login.
// Keep only endpoints for user profile management or Supabase JWT verification if needed.

import { Router } from 'express';

const router = Router();

// All legacy auth endpoints have been removed. This router is intentionally left empty for backward compatibility.

export default router; 